# Server Configuration
PORT=5000
NODE_ENV=development

# PostgreSQL Database
DB_HOST=localhost
DB_PORT=5432
DB_NAME=parking_db
DB_USER=postgres
DB_PASSWORD=DB_PASSWORD

# JWT Secret (for API tokens and QR codes)
JWT_SECRET=your_super_secret_jwt_key_change_this_in_production

# Firebase Admin SDK Configuration
# 
# DEVELOPMENT (Local): Use service account key file
FIREBASE_SERVICE_ACCOUNT_PATH=./firebase-service-account.json
FIREBASE_PROJECT_ID=your-firebase-project-id
#
# PRODUCTION (Deployment): Use environment variables instead of file
# Uncomment and set these in your hosting platform (Render, Railway, etc.)
# FIREBASE_PROJECT_ID=your-project-id
# FIREBASE_PRIVATE_KEY_ID=your-private-key-id
# FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n"
# FIREBASE_CLIENT_EMAIL=firebase-adminsdk-xxxxx@your-project.iam.gserviceaccount.com
# FIREBASE_CLIENT_ID=your-client-id
# FIREBASE_CERT_URL=https://www.googleapis.com/robot/v1/metadata/x509/firebase-adminsdk-xxxxx@your-project.iam.gserviceaccount.com
#
# ALTERNATIVE: Use base64 encoded service account (simpler for some platforms)
# FIREBASE_SERVICE_ACCOUNT_BASE64=base64_encoded_json_here

# Frontend URL (for CORS)
FRONTEND_URL=http://localhost:3000
